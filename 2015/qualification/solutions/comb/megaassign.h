#pragma once

#include "../../base/common.h"

vector<int> ser_to_pool(const Input& input, const vector<vector<int>>& rows) {
  return {};
  /*
  vector<int> ans(input.M, -1);
  int now;
  int pl = 0;
  {
    now = 14;
    for (int i = 0; i < rows[now]; ++i) {
      ans[rows[now]] = pl;
    }
  }
  {
    now = 5;
    for (int i = 0; i < rows[now]; ++i) {
      ans[rows[now]] = pl;
    }
  }
  return ans;
  */
}
