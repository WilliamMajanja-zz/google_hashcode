#pragma once

#include "../../base/solution.h"

using Pos = std::pair<int, int>;

struct RideInfo {
    Pos start_pos_;
    Pos finish_pos_;

    int min_start_time_;
    int max_finish_time_;
};

struct CarInfo {
    std::vector<int> rides_in_progress_;
    std::vector<int> rides_finished_;
};

class Solution : public BaseSolution {
public:
  template <typename... T>
  Solution(T&&... args):
      BaseSolution(std::forward<T>(args)...) {}

  void solve_internal(const Input& input, Output& output) override {
    /* solve test here */
  }

private:
  const string class_name_ = "Solution";
};

